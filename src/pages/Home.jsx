import {useState} from 'react'
import {useNavigate} from 'react-router-dom'
import Button from '../components/Button'
export default function Home(){const[alias,setAlias]=useState('');const navigate=useNavigate();function start(e){e.preventDefault();sessionStorage.setItem('playerAlias',alias.trim()||'Ashen');navigate('/game/1')}return <main className="screen home"><section className="hero panel"><p className="eyebrow">POKEMON ROUTE / LEVEL 01</p><h1>PALLET TOWN RUN</h1><p className="lead">Corre por la ruta, recoge todas las Poké Balls y llega a la bandera antes de que se acabe el tiempo.</p><form onSubmit={start} className="start-form"><label htmlFor="alias">Nombre del entrenador</label><input id="alias" value={alias} onChange={e=>setAlias(e.target.value)} maxLength={16} placeholder="Escribe tu nombre"/><Button type="submit">Jugar la ruta</Button></form><div className="rules"><span>3 vidas</span><span>8 Poké Balls</span><span>5 Rattata</span><span>120 segundos</span></div></section></main>}