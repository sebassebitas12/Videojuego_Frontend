import {useState} from 'react'
import {useNavigate} from 'react-router-dom'
import Button from '../components/Button'
export default function Home(){const[alias,setAlias]=useState('');const navigate=useNavigate();function start(e){e.preventDefault();sessionStorage.setItem('playerAlias',alias.trim()||'Ashen');navigate('/game/1')}return <main className="screen home"><section className="hero panel"><p className="eyebrow">DUNGEON RUN / LEVEL 01</p><h1>REALM OF ASHES</h1><p className="lead">Entra a la cripta, recupera la llave y encuentra la salida antes de que el tiempo se consuma.</p><form onSubmit={start} className="start-form"><label htmlFor="alias">Alias del jugador</label><input id="alias" value={alias} onChange={e=>setAlias(e.target.value)} maxLength={16} placeholder="Escribe tu alias"/><Button type="submit">Entrar al dungeon</Button></form><div className="rules"><span>3 vidas</span><span>3 enemigos</span><span>1 llave</span><span>60 segundos</span></div></section></main>}